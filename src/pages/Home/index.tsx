import MainLayout from '../../components/layout';
import { Box } from '@mui/material';

const HomePage = () => {
  return (
    <MainLayout>
      <Box>HOME</Box>
    </MainLayout>
  );
};

export default HomePage;
